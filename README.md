# idontwanttomovemyeyes

No install needed. It's a single HTML file.

1. Clone the repo:
   ```bash
   git clone https://github.com/ShannonLin284/idontwanttomovemyeyes.git
   ```
2. Open `index.html` in any browser — double-click it or drag it into a tab.

That's it.